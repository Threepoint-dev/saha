# App name
spring.application.name=saha

# Supabase PostgreSQL connection
spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres?prepareThreshold=0
spring.datasource.username=postgres.gduznethdrcjclifuiwt
spring.datasource.password=ZrAFHGfWw08prnrF
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Server port
server.port=8080

spring.datasource.hikari.max-lifetime=180000
spring.datasource.hikari.idle-timeout=120000
spring.datasource.hikari.keepalive-time=90000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.validation-timeout=5000

# Supabase JWT verification — public keys only, no secret to leak.
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=https://gduznethdrcjclifuiwt.supabase.co/auth/v1/.well-known/jwks.json